# This file is used to manually specify the semi-automatic conversion of
# 'nipype.interfaces.fsl.model.ContrastMgr' from Nipype to Pydra.
#
# Please fill-in/edit the fields below where appropriate
#
# Docs
# ----
# Use FSL contrast_mgr command to evaluate contrasts
# 
#     In interface mode this file assumes that all the required inputs are in the
#     same location. This has deprecated for FSL versions 5.0.7+ as the necessary
#     corrections file is no longer generated by FILMGLS.
#     
task_name: ContrastMgr
nipype_name: ContrastMgr
nipype_module: nipype.interfaces.fsl.model
inputs:
  omit:
  # list[str] - fields to omit from the Pydra interface
  rename:
  # dict[str, str] - fields to rename in the Pydra interface
  types:
  # dict[str, type] - override inferred types (use "mime-like" string for file-format types,
  # e.g. 'medimage/nifti-gz'). For most fields the type will be correctly inferred
  # from the nipype interface, but you may want to be more specific, particularly
  # for file types, where specifying the format also specifies the file that will be
  # passed to the field in the automatically generated unittests.
    corrections: generic/file
    # type=file|default=<undefined>: statistical corrections used within FILM modelling
    dof_file: generic/file
    # type=file|default=<undefined>: degrees of freedom
    fcon_file: generic/file
    # type=file|default=<undefined>: contrast file containing F-contrasts
    param_estimates: generic/file+list-of
    # type=inputmultiobject|default=[]: Parameter estimates for each column of the design matrix
    sigmasquareds: generic/file
    # type=file|default=<undefined>: summary of residuals, See Woolrich, et. al., 2001
    tcon_file: generic/file
    # type=file|default=<undefined>: contrast file containing T-contrasts
  callable_defaults:
  # dict[str, str] - names of methods/callable classes defined in the adjacent `*_callables.py`
  # to set as the `default` method of input fields
  metadata:
  # dict[str, dict[str, any]] - additional metadata to set on any of the input fields (e.g. out_file: position: 1)
outputs:
  omit:
  # list[str] - fields to omit from the Pydra interface
  rename:
  # dict[str, str] - fields to rename in the Pydra interface
  types:
  # dict[str, type] - override inferred types (use "mime-like" string for file-format types,
  # e.g. 'medimage/nifti-gz'). For most fields the type will be correctly inferred
  # from the nipype interface, but you may want to be more specific, particularly
  # for file types, where specifying the format also specifies the file that will be
  # passed to the field in the automatically generated unittests.
    copes: generic/file+list-of
    # type=outputmultiobject: Contrast estimates for each contrast
    fstats: generic/file+list-of
    # type=outputmultiobject: f-stat file for each contrast
    neffs: generic/file+list-of
    # type=outputmultiobject: neff file ?? for each contrast
    tstats: generic/file+list-of
    # type=outputmultiobject: t-stat file for each contrast
    varcopes: generic/file+list-of
    # type=outputmultiobject: Variance estimates for each contrast
    zfstats: generic/file+list-of
    # type=outputmultiobject: z-stat file for each F contrast
    zstats: generic/file+list-of
    # type=outputmultiobject: z-stat file for each contrast
  callables:
  # dict[str, str] - names of methods/callable classes defined in the adjacent `*_callables.py`
  # to set to the `callable` attribute of output fields
  templates:
  # dict[str, str] - `output_file_template` values to be provided to output fields
  requirements:
  # dict[str, list[str]] - input fields that are required to be provided for the output field to be present
tests:
- inputs:
  # dict[str, str] - values to provide to inputs fields in the task initialisation
  # (if not specified, will try to choose a sensible value)
    tcon_file:
    # type=file|default=<undefined>: contrast file containing T-contrasts
    fcon_file:
    # type=file|default=<undefined>: contrast file containing F-contrasts
    param_estimates:
    # type=inputmultiobject|default=[]: Parameter estimates for each column of the design matrix
    corrections:
    # type=file|default=<undefined>: statistical corrections used within FILM modelling
    dof_file:
    # type=file|default=<undefined>: degrees of freedom
    sigmasquareds:
    # type=file|default=<undefined>: summary of residuals, See Woolrich, et. al., 2001
    contrast_num:
    # type=range|default=1: contrast number to start labeling copes from
    suffix:
    # type=str|default='': suffix to put on the end of the cope filename before the contrast number, default is nothing
    output_type:
    # type=enum|default='NIFTI'|allowed['NIFTI','NIFTI_GZ','NIFTI_PAIR','NIFTI_PAIR_GZ']: FSL output type
    args:
    # type=str|default='': Additional parameters to the command
    environ:
    # type=dict|default={}: Environment variables
  imports:
  # list[nipype2pydra.task.base.importstatement] - list import statements required by the test, with each list item
  # consisting of 'module', 'name', and optionally 'alias' keys
  expected_outputs:
  # dict[str, str] - expected values for selected outputs, noting that tests will typically
  # be terminated before they complete for time-saving reasons, and therefore
  # these values will be ignored, when running in CI
  timeout: 10
  # int - the value to set for the timeout in the generated test, 
  # after which the test will be considered to have been initialised 
  # successfully. Set to 0 to disable the timeout (warning, this could
  # lead to the unittests taking a very long time to complete)
  xfail: true
  # bool - whether the unittest is expected to fail or not. Set to false
  # when you are satisfied with the edits you have made to this file
doctests: []
